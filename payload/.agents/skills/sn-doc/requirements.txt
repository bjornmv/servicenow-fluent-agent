# sn-doc Python dependencies
# Mirrors C:\Personal\SNagent\requirements-sn-doc.txt — pinned floors verified on Windows.

docutils>=0.22
myst-parser>=4.0
python-docx>=1.2
lxml>=4.9
weasyprint>=68
premailer>=3.10

# Optional — only needed if you wire in Playwright screenshot capture later.
# playwright>=1.40
